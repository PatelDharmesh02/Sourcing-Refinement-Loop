GEMINI_API_KEY=
# Deployed API. Clear or remove this line to fall back to local uvicorn (Vite proxies /api → :8000).
VITE_API_BASE_URL=https://sourcing-refinement-loop-utg8.vercel.app
# Optional comma-separated browser origins allowed by the API (defaults include localhost + the Vercel UI).
# CORS_ORIGINS=http://localhost:5173,https://sourcing-refinement-loop-chi.vercel.app
